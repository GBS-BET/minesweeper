class BookAMeeting extends BaseButton {
    constructor ({el, innerText, parent}) {
        super({el, parent});

        this.el.innerText = innerText;

        // To add css class


        // To new  css class after Event 
    }
}