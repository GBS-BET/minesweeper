class ButtonContainer extends BaseCustomControl {
    constructor ({el, parent}) {
        super({el, parent});

        
        // To add css class

        this.el.classList.add('dsp-flex');
    }
}