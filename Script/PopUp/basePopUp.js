class BasePopUp extends BaseCustomControl {
    constructor ({el, parent}) {
        super({el, parent});

        //this.levelHeader = new LevelHeader({el: 'level-header', parent: this});
      
       
    }
}