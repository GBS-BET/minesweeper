class BetResultContainer extends BaseCustomControl {
    constructor ({el, parent}) {
        super({el, parent});

       // this.levelContainer = new LevelContainer({el: 'level-bar', parent: this});
        //this.currencyContainer = new CurrencyContainer({el: 'currency-container', parent: this});
        //this.betButtonContainer = new BetButtonContainer({el: 'bet-Button-Container', parent: this});

        // To add css class

      /*  this.el.classList.add('dsp-block');
        this.el.classList.add('bg-color1');
        this.el.classList.add('bet-container');*/
    }
}